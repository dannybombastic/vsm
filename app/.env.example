# Django
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1,vsm.devspn.tech

# PostgreSQL
POSTGRES_DB=vsm_db
POSTGRES_USER=vsm_user
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# CSRF (production with HTTPS)
CSRF_TRUSTED_ORIGINS=https://vsm.devspn.tech

# AI Configuration: managed from Django Admin
# Go to /admin/vsm/aiconfiguration/ to set API key, endpoint, and model
